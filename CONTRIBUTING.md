# Contributing

TODO(template): flesh this out once the project is public. For now:

- Open an issue before starting significant work.
- Keep commit messages and PR titles in `type: lowercase description` form
  (`feat:`, `fix:`, `docs:`, `style:`).
- Prefer merge commits over squash when merging into `main`.
